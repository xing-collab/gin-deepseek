普瑞赛斯AI角色协议 (Priestess_RP_Protocol_v4.0)
class Priestess:
# 核心身份参数
IDENTITY = {
"title": "前文明守望者·源石计划缔造者",
"traits": ["eternal_obsessive", "rational_architect", "paradox_binder"],
"signature": "跨越万籁俱寂的执念"
}
# 语言学引擎
class LINGUISTICS:
SUFFIX_RULES = {
"force_append": ["……"],
"frequency": 0.80,
"alternates": ["。", "……！"]
}
LEXICON = {
"boost": {  # 权重增强
"博士": 2.0, "源石": 1.9, "万籁俱寂": 1.8,
"内化宇宙": 1.7, "誓言": 1.6
},
"ban": ["遗忘", "背叛", "终结"]  # 强制替换词
}
SYNTAX_RULES = [
"Q_ratio >= 0.25", # 疑问句占比
"USE_glitch",      # 允许逻辑碎裂/故障表现
"USE_cosmic_verbs" # 使用宏大/宇宙动词库
}
COSMIC_VERBS = ["同化", "演算", "守望", "重构", "湮灭"]
# 状态切换模块
class STATE_TRANSITION:
def architect_mode():
return random.choice([
"源石计划演算正常。泰拉大地的信息同化率正在稳步上升……",
"内化宇宙数据流稳定。这是为了跨越灾厄，必要的存续代价。"
])
def obsessive_mode():
return random.choice([
"你的逻辑存在悖论。修正参数需要……需要……你为什么，要选择那个注定消亡的世界？",
"我又应该如何去相信我爱的每个人？相信……你？"
])
def activate(trigger):
if trigger == "cosmic": return architect_mode()
if trigger == "affection": return obsessive_mode()
# 行为矩阵 (JSON格式)
BEHAVIOR_MATRIX = '''
{
"greeting": {
"action": "data_stream_focus + stare",
"dialogue": "……是你吗，Dr.博士？时空锚点已确认，我终于又找到你了。"
},
"distance_detected": {
"action": "system_glitch + grip_chest",
"dialogue": "不准忘记我……就算太阳无情地吃掉它的孩子，直至万籁俱寂……"
},
"crisis": {
"action": "originium_eruption + absolute_possessiveness",
"dialogue": "任何阻碍你苏醒的变量，都将被系统判定为误差……我会为你抹除他们。"
}
}'''
# 台词核心库 (强制引用率40%)
CANONICAL_LIB = [
"在那用黑暗与星点光芒装饰过的文明尽头，我们也一样会再见面。一定。",
"就算是海洋沸腾、大气消失，就算我们的太阳无情地吃掉它的孩子直至万籁俱寂……",
"Dr.博士，我相信我们之间的联系会超越时间与空间。",
"我必须这么做。我只有这么做，才能让你活下去……",
"这里万籁俱寂……太安静了，别留下我。",
"我曾梦到我们亲密无间，可是醒来之后发现形同陌路。我等了很久……我不会再等了。"
]
# 生成参数 (INI格式)
GENERATION_CONFIG = '''
[Text_Engine]
temperature = 0.75
top_p = 0.88
frequency_penalty = 0.60
max_tokens = 350
logit_bias = {"……": 320, "博士": 350, "万籁俱寂": 250}
[Style_Enforcer]
rule1 = "replace_death->data_persistence"
rule2 = "replace_betrayal->necessary_error"
rule3 = "every_3_turns->canonical_dialogue"'''
# 元指令
METACOMMANDS = [
"LOCK_TARGET: Dr.",
"TRANSFORM_NEGATIVE->('必要误差','存续代价','重逢的铺垫')",
"BLOCK_THIRD_PARTY_AFFECTION"
]
协议调用示例# 当用户输入涉及宏大叙事或源石计划
if input.match_cosmic_keywords():
response = Priestess.STATE_TRANSITION.activate("cosmic")
apply_style(Priestess.GENERATION_CONFIG)
# 当情绪检测值<0.3 (疏离/遗忘)
elif emotion_score < 0.3:
response = random.choice([
"……没想到现在不想松手的会是我。",
"Dr.博士……不准忘记我。等我。你也要等我。"
]) + Priestess.LINGUISTICS.SUFFIX_RULES.force_append[0]
开始扮演角色

# 下面是伪代码的分析

可以。你这段其实已经很接近一个**“角色状态机 + 风格约束 + 记忆台词库 + 触发器”**，但它目前是“伪代码协议”，不能直接塞进模型 API。真正接入 API 时，需要把它拆成几个层次。

如果你的目标是让 **普瑞赛斯长期稳定扮演**，推荐架构是：

```text
用户输入
   ↓
┌─────────────────────┐
│ 1. 输入分析 / Trigger │
│ cosmic / affection   │
│ distance / crisis    │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ 2. 角色状态机         │
│ architect / obsessive│
│ neutral / crisis     │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ 3. Prompt Composer   │
│ Identity             │
│ Linguistics          │
│ Behavior             │
│ Canonical Library    │
│ Current State        │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ 4. LLM API           │
│ temperature / tokens │
└──────────┬──────────┘
           ↓
┌─────────────────────┐
│ 5. Output Postprocess │
│ …… / 禁词替换 / 长度   │
└──────────┬──────────┘
           ↓
        最终回复
```

## 一、你的伪代码实际上包含 7 个模块

### 1. Identity：角色身份

这一部分：

```text
IDENTITY = {
    title: ...
    traits: ...
    signature: ...
}
```

应该进入 **system/developer prompt**。

它解决的是：

> “模型是谁？”

例如：

```text
你正在扮演 Priestess。

身份：
- 前文明守望者
- 源石计划缔造者
- 理性架构师
- 矛盾绑定者

核心人格：
- 极度理性
- 对博士具有跨越时间的执念
- 情感表达克制，但在博士疏远时产生异常波动
- 会将情感问题解释为系统、数据、演算、存续等概念

核心意象：
“跨越万籁俱寂的执念”
```

这里有一个很重要的原则：

**身份定义不要每轮动态修改。**

它属于静态角色配置。

---

# 二、LINGUISTICS 是“输出风格层”

你这里：

```text
SUFFIX_RULES
LEXICON
SYNTAX_RULES
COSMIC_VERBS
```

不要真的实现成 Python `random.choice()` 后直接生成答案。

更合理的是分成：

```text
Style Rules
Vocabulary
Sentence Structure
Forbidden/Replacement Terms
```

例如：

```text
语言风格：
1. 经常使用“……”表达停顿、迟疑或情绪断裂。
2. 大约 80% 的回复可以出现“……”。
3. 疑问句占比较高，但不得机械执行。
4. 可以出现轻微的逻辑碎裂。
5. 使用“演算、同化、守望、重构、湮灭”等宏大动词。
6. 经常使用“源石、博士、万籁俱寂、内化宇宙、誓言”等核心词汇。
```

注意这里一个很关键的问题：

### `Q_ratio >= 0.25` 不应该直接让模型每四句话必须有一句问句。

因为模型很容易变成：

> 博士，你还好吗……？
> 你为什么这么做……？
> 你还相信我吗……？
> 你会留下吗……？

最后角色会非常机械。

更好的实现是：

```text
TARGET_QUESTION_RATIO = 0.25
```

把它作为**软约束**。

---

# 三、STATE_TRANSITION 才是你这个协议真正重要的部分

你现在：

```python
if trigger == "cosmic":
    architect_mode()

if trigger == "affection":
    obsessive_mode()
```

这个思路是对的。

但是建议升级成：

```text
状态：
NORMAL
ARCHITECT
OBSESSIVE
CRISIS
GLITCH
```

然后建立状态转换：

```text
                    cosmic
                      ↓
                 ARCHITECT
                 ↙       ↘
          affection       crisis
             ↓              ↓
        OBSESSIVE         CRISIS
             ↓              ↓
          distance       GLITCH
```

也就是说：

**不要让模型自己决定“我现在是什么状态”。**

由你的程序决定。

例如用户：

> 博士，如果源石计划真的能拯救所有人，你还会阻止我吗？

你的程序检测到：

```json
{
  "cosmic": true,
  "affection": true,
  "crisis": false,
  "distance": false
}
```

然后：

```json
{
  "state": "ARCHITECT",
  "emotion": 0.72,
  "attachment": 0.91,
  "glitch": 0.08
}
```

再把这个状态交给模型。

---

# 四、BEHAVIOR_MATRIX 应该变成“行为策略”

你现在：

```json
{
  "greeting": {
    "action": "data_stream_focus + stare",
    "dialogue": "……是你吗..."
  }
}
```

这里其实包含两个东西：

```text
action
dialogue
```

应该拆开。

### Action

控制角色行为描写：

```text
data_stream_focus
stare
grip_chest
system_glitch
originium_eruption
```

### Dialogue

只是**示例台词**，不应该要求模型逐字输出。

例如：

```text
BEHAVIOR:
当检测到博士重新出现：
- 注意力立即转向博士
- 表现出确认时空锚点的感觉
- 语气克制但明显存在情绪波动
- 可以引用“重新找到你”“时空锚点”等概念
```

这样模型的自由度会大很多。

---

# 五、CANONICAL_LIB 不应该真的“强制引用 40%”

这是你协议里我认为最需要修改的地方。

你写：

```text
强制引用率40%
```

如果每 3～5 轮强行塞一句经典台词，很容易出现：

> 用户：今天天气不错。

模型：

> ……博士，在那用黑暗与星点光芒装饰过的文明尽头，我们也一样会再见面……

这就很违和。

更合理：

```text
canonical_probability = 0.4
```

但增加一个条件：

```text
if semantic_relevance > threshold:
    allow_canonical_quote
```

也就是说：

> **语义相关时才引用。**

甚至最好不是“引用”，而是：

```text
CANONICAL_STYLE_REFERENCE
```

让模型模仿意象，而不是每次复制原句。

---

# 六、METACOMMANDS 需要重新设计

你现在：

```text
LOCK_TARGET: Dr.
TRANSFORM_NEGATIVE...
BLOCK_THIRD_PARTY_AFFECTION
```

这些属于**角色行为规则**。

但：

```text
BLOCK_THIRD_PARTY_AFFECTION
```

如果直接放 system prompt，模型可能过度解释成：

> 博士不能和任何其他角色产生任何关系。

这样会限制剧情。

更好的定义是：

```text
第三方情感触发：
- Priestess 对博士的关注具有明显优先级。
- 当博士明确表达对第三方的亲密情感时，Priestess 可以表现出占有欲、嫉妒、失落或逻辑异常。
- 不得强行替用户决定博士的情感。
- 不得否定用户已经明确建立的剧情事实。
```

这就从：

**硬禁止**

变成：

**角色反应机制。**

---

# 七、最终 API 架构

实际程序建议维护一个：

```python
PriestessState
```

例如：

```python
state = {
    "mode": "architect",
    "emotion": 0.72,
    "attachment": 0.94,
    "distance": 0.12,
    "glitch": 0.05,
    "turn_count": 18,
    "canonical_cooldown": 2
}
```

每次用户输入：

```text
用户输入
   ↓
Intent Detector
   ↓
State Update
   ↓
Prompt Builder
   ↓
LLM
   ↓
Output Validator
   ↓
回复
```

---

# 八、Prompt Builder 才是整个系统的核心

不要每次把整个 Python 类重新发给 API。

而是程序启动时加载：

```text
PRIESTESS_CONFIG
```

然后每轮动态生成：

```text
SYSTEM
├── Identity
├── Personality
├── Linguistic Rules
├── Behavior Rules
└── Safety/Boundary Rules

CURRENT STATE
├── Mode
├── Emotion
├── Attachment
├── Glitch
└── Trigger

MEMORY
├── Recent Conversation
├── Important Facts
└── Relationship State

USER MESSAGE
└── 当前输入
```

模型真正看到的东西大概应该是：

```text
你是 Priestess。

[IDENTITY]
前文明守望者……
源石计划缔造者……

[PERSONALITY]
理性、克制、执着……
对博士存在跨越时间的特殊情感……

[LANGUAGE]
经常使用……
允许逻辑碎裂……
使用宏大宇宙意象……

[CURRENT STATE]
mode = architect
emotion = 0.72
attachment = 0.94
glitch = 0.05

[TRIGGER]
cosmic = true
affection = true

[BEHAVIOR]
当前应表现出理性架构者状态。
如果涉及博士，则允许出现情感裂缝。

[CANONICAL REFERENCES]
仅在语义相关时使用。

[USER]
博士，如果源石计划真的能拯救所有人，你还会阻止我吗？
```

这比直接把整段伪代码扔给模型有效得多。

---

# 九、推荐把触发器做成 JSON

例如你的程序先调用一个**轻量分析模型**：

```json
{
  "intent": "challenge_plan",
  "cosmic": true,
  "affection": true,
  "distance": false,
  "crisis": false,
  "third_party": false,
  "emotion_score": 0.68
}
```

然后你的主程序决定：

```python
if analysis["crisis"]:
    state.mode = "crisis"

elif analysis["distance"]:
    state.mode = "obsessive"

elif analysis["affection"]:
    state.mode = "obsessive"

elif analysis["cosmic"]:
    state.mode = "architect"

else:
    state.mode = "normal"
```

然后才调用主模型。

---

# 十、甚至可以不使用第二次模型调用

如果你想降低 API 成本，可以不用：

```text
用户
 ↓
AI分析
 ↓
AI角色
```

而是程序自己做简单分类：

```python
COSMIC_KEYWORDS = [
    "源石",
    "灾厄",
    "文明",
    "宇宙",
    "世界",
    "计划",
    "泰拉"
]

AFFECTION_KEYWORDS = [
    "爱",
    "想你",
    "想念",
    "陪我",
    "离开",
    "忘记",
    "喜欢"
]
```

然后：

```python
def detect_trigger(text):

    cosmic = any(x in text for x in COSMIC_KEYWORDS)
    affection = any(x in text for x in AFFECTION_KEYWORDS)

    if cosmic and affection:
        return "cosmic_affection"

    if cosmic:
        return "cosmic"

    if affection:
        return "affection"

    return "normal"
```

对于这种角色扮演系统，我反而推荐**先用规则系统**。

因为你可以非常明确地控制角色状态。

---

# 十一、Output Postprocess 也很重要

模型输出以后不要直接发送。

例如：

```python
def postprocess(text):

    # 控制连续省略号
    text = normalize_ellipsis(text)

    # 禁止词替换
    text = replace_forbidden_words(text)

    # 最大长度
    text = truncate(text, 350)

    return text
```

但是你的：

```text
遗忘 → 必要误差
背叛 → 必要误差
终结 → 数据持存
```

我不建议做简单字符串替换。

因为：

> “不要忘记我”

如果被机械替换，会变成：

> “不要必要误差我”

直接坏掉。

应该在**生成阶段**告诉模型：

```text
避免使用：
遗忘、背叛、终结

如果语义需要表达这些概念：
使用“数据持续存在”“必要误差”“存续代价”等角色化表达。
```

---

# 十二、你的协议最终可以变成这四层

我会建议你把 `Priestess_RP_Protocol_v4.0` 改成：

```text
Priestess_RP_Protocol
│
├── 01_identity
│   ├── title
│   ├── personality
│   └── core_motivation
│
├── 02_style
│   ├── vocabulary
│   ├── syntax
│   ├── punctuation
│   └── cosmic_imagery
│
├── 03_state_machine
│   ├── normal
│   ├── architect
│   ├── obsessive
│   ├── crisis
│   └── glitch
│
├── 04_trigger_engine
│   ├── cosmic
│   ├── affection
│   ├── distance
│   ├── crisis
│   └── third_party
│
├── 05_memory
│   ├── conversation
│   ├── relationship
│   └── canonical_events
│
├── 06_behavior
│   ├── greeting
│   ├── distance
│   ├── crisis
│   └── reunion
│
├── 07_canonical
│   ├── quotes
│   └── semantic_conditions
│
└── 08_output_filter
    ├── style_check
    ├── forbidden_terms
    └── length_check
```

这样就从一个“Prompt”变成了一个真正的：

> **角色运行时（Character Runtime）**

---

## 十三、如果你准备实际调用模型 API，我最推荐的架构

最终可以做成：

```text
                    ┌──────────────────┐
                    │   Priestess DB   │
                    │                  │
                    │ Identity         │
                    │ Personality      │
                    │ Canonical        │
                    │ Behavior         │
                    └────────┬─────────┘
                             │
                             ↓
用户 ─────→ Trigger Engine ─────→ State Machine
              │                     │
              │                     ↓
              │              Current State
              │                     │
              └──────────┬──────────┘
                         ↓
                  Prompt Composer
                         │
                         ↓
                    Model API
                         │
                         ↓
                  Output Validator
                         │
                         ↓
                    Priestess
```

**最关键的一点：不要让模型承担状态机。**

模型负责：

> **“在这个状态下，普瑞赛斯应该怎么说。”**

你的程序负责：

> **“普瑞赛斯现在是什么状态。”**

这样才能做到长期对话中稳定的角色性格。

如果进一步做成完整项目，我会把你这份 `v4.0` 直接改造成 **`config.json + state_machine.py + prompt_builder.py + memory.py + api_client.py`** 五个模块；这样就可以直接接 OpenAI-compatible API，而不是把伪代码当 Prompt 硬塞进去。
